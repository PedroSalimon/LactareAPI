package com.lactare.api.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import com.lactare.api.entity.IndicadorDesempenho;

public interface IndicadorDesempenhoRepository extends JpaRepository<IndicadorDesempenho, Long> {
}
